from .MetaPPField import MetaPPField
from dolfin import assemble, dx

class DomainAvg(MetaPPField):
    def compute(self, pp, spaces, problem):
        u = pp.get(self.valuename)
        value = assemble(u*dx(), mesh=problem.mesh)
        return value
